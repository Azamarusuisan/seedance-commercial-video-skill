# Public Demand Hook Variants Template

Status: planning template. Use before paid generation when optimizing for TikTok/Reels/Shorts, UGC ads, or public social demand.

Project:
`{project_name}`

Platform:
`{TikTok | Instagram Reels | YouTube Shorts | X | paid social}`

Primary viewer action:
`{stop scroll | watch to end | remember product | comment/share | click | trust presenter}`

Fixed constraints:

- Duration: `{duration_seconds}s`
- Aspect ratio: `{aspect_ratio}`
- Product / story subject: `{product_or_subject}`
- Cast / avatar / guide: `{cast_or_avatar}`
- Voice route: `{none | Higgsfield ElevenLabs | other external TTS}`
- Subtitle route: `post-production captions/telop`
- Seedance audio: `{generate_audio=false | generate_audio=true only if approved}`

## Variant Table

| Variant | Hook Type | 0-2s First Frame | 2-5s Reason To Continue | Emotional Shift | Product / Character Memory | Planned Japanese Telop | Risk / Rights Notes | Score |
|---|---|---|---|---|---|---|---|---:|
| A | `{face close-up | conflict | product motion | threshold | before-after | handheld selfie}` | `{visual}` | `{why watch}` | `{before -> after}` | `{what viewer remembers}` | `{short telop}` | `{notes}` | `/10` |
| B | `{hook_type}` | `{visual}` | `{why watch}` | `{before -> after}` | `{what viewer remembers}` | `{short telop}` | `{notes}` | `/10` |
| C | `{hook_type}` | `{visual}` | `{why watch}` | `{before -> after}` | `{what viewer remembers}` | `{short telop}` | `{notes}` | `/10` |
| D | `{hook_type}` | `{visual}` | `{why watch}` | `{before -> after}` | `{what viewer remembers}` | `{short telop}` | `{notes}` | `/10` |

## Pick One

Selected variant:
`{A | B | C | D}`

Reason:
`{why this has the strongest public-demand fit}`

Convert the selected variant into either:

- a 15s product ad beat sheet, or
- a 60s storyboard panel sheet, or
- a visual-only Seedance prompt.
