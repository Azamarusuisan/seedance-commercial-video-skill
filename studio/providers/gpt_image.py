class GPTImageProvider:
    def estimate(self, **_kwargs):
        raise NotImplementedError("GPT Image real provider is disabled until OPEN-2 is resolved.")

    def generate(self, **_kwargs):
        raise NotImplementedError("GPT Image real provider is disabled until OPEN-2 is resolved.")
