class SeedanceProvider:
    def estimate(self, **_kwargs):
        raise NotImplementedError("Seedance real provider is disabled until OPEN-2 is resolved.")

    def generate(self, **_kwargs):
        raise NotImplementedError("Seedance real provider is disabled until OPEN-2 is resolved.")
