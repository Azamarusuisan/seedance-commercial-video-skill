{
  "type": "object",
  "required": ["at", "gate", "project", "target", "target_sha256", "verdict", "by", "event_id"],
  "properties": {
    "at": {"type": "string"},
    "gate": {"type": "string"},
    "project": {"type": "string"},
    "target": {"type": "string"},
    "target_sha256": {"type": "string"},
    "verdict": {"type": "string", "enum": ["approved", "rejected", "revoked"]},
    "by": {"type": "string"},
    "note": {"type": "string"},
    "event_id": {"type": "string"}
  }
}
